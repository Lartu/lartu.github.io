import "web.kat";

# Footer
exec("date");
$datetime: trim($_stdout);

print(
    center(parc(
        small("Webpage compiled on ", $datetime, ".<br>If it's been a while
        since that date, refresh the page to see if there's something new!
        <br>(but probably not)"))
    )
);