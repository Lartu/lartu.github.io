import "web.kat";

# Footer
exec("date");
$datetime: trim($_stdout);

print(
    center(
        parc(small("Webpage compiled on ", $datetime, ".")),
        link(img("images/katbadge.png"), "https://github.com/Lartu/katalyn", $true),
    )
);